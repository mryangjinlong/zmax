package com.zmaxfilm.service.impl;

import com.zmaxfilm.Constant;
import com.zmaxfilm.model.User;
import com.zmaxfilm.service.UserService;
import com.zmaxfilm.util.EhcacheUtil;

/**
 * 用户服务
 * Created by jimmy on 2016/12/12.
 */
public class UserServiceImpl implements UserService{

    private EhcacheUtil ehcacheUtil = EhcacheUtil.getInstance();
    /**
     * 会员卡登录
     * @param cardId   会员卡号
     * @param password      密码
     * @return  User
     */
    @Override
    public User cardLogin(String cardId, String password) {
        return null;
    }

    /**
     * 手机号码登录
     * @param phoneNum  手机号码
     * @param password  密码
     * @return User
     */
    @Override
    public User mobileLogin(String phoneNum, String password) {
        return null;
    }

    /**
     * 扫二维码登录
     * @return User
     */
    @Override
    public User scanCodeLogin() {
        return null;
    }

    /**
     * 短信验证登录
     * @param phoneNum  手机号
     * @param code       验证码
     * @return User
     */
    @Override
    public User SMSLogin(String phoneNum, String code) {
        return null;
    }

    @Override
    public User getUser() {
        return (User)ehcacheUtil.get(Constant.CACHE_KEY_USER);
    }
}
